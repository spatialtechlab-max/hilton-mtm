node_modules
.next
out
.env*
.DS_Store
*.log
.vercel
.turbo

# Project handbook with credentials — gitignored so secrets don't leave the laptop.
CLAUDE.md

# Local backups of original product photos (before bg removal).
# Re-download from hiltonmtm.com if needed.
public/products/*-bak/


# TypeScript incremental build cache
tsconfig.tsbuildinfo
public/booklet/

# Generated deliverables (exam answer key, email previews) — keep out of the public repo
docs/
